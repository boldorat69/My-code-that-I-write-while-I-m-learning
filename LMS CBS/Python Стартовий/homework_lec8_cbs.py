# Завдання 1
# Створіть функцію, яка відображає привітання для користувача із заданим імям. Якщо імя не вказано, вона повинна
# виводити привітання для користувача з Вашим ім'ям.


def hello_user(name="Валерій"):
    print(f"Привіт, {name}!")


user_name = input("Введіть своє ім'я (або натисніть Enter): ")

if user_name:
    hello_user(user_name)
else:
    hello_user()


# Завдання 2
# Створіть дві функції, що обчислюють значення певних алгебраїчних виразів. На екрані виведіть таблицю значень
# цих функцій від -5 до 5 з кроком 0.5.
def f1(x):
    return x**2 - 2 * x + 3


def f2(x):
    return x**3 - x


print(f"{'x':>6} {"f1(x)":>10} {"f2(x)":>10}")
print("-" * 30)
x = -5.0
while x <= 5.0:
    print(f"{x:6.1f} {f1(x):10.2f} {f2(x):10.2f}")
    x += 0.5
print("-" * 30)

# Завдання 3
# Створіть програму-калькулятор, яка підтримує наступні операції: додавання, віднімання, множення, ділення,
# зведення в ступінь, зведення до квадратного та кубічного коренів. Всі дані повинні вводитися в циклі,
# доки користувач не вкаже, що хоче завершити виконання програми. Кожна операція має бути реалізована у вигляді
# окремої функції. Функція ділення повинна перевіряти дані на коректність та видавати повідомлення про помилку
# у разі спроби поділу на нуль.
import math

# --- Функції арифметичних операцій ---
def add(a, b):
    return a + b

def subtract(a, b):
    return a - b

def multiply(a, b):
    return a * b

def divide(a, b):
    if b == 0:
        return "Ділення на нуль!"
    return a / b

def sqrt2(a):
    if a < 0:
        return "Підкореневе число від’ємне!"
    return math.sqrt(a)

def sqrt3(a):
    return round(a ** (1/3), 6)  # кубічний корінь

while True:
    print("\n🔢 Меню калькулятора:")
    print("1 - Додавання (+)")
    print("2 - Віднімання (-)")
    print("3 - Множення (*)")
    print("4 - Ділення (/)")
    print("5 - Зведення у ступінь (a^b)")
    print("6 - Квадратний корінь (√a)")
    print("7 - Кубічний корінь (∛a)")
    print("0 - Вихід")

    choice = input("Виберіть операцію: ")
    if choice == "0":
        print("До побачення.")
        break
    elif choice in ["1", "2", "3", "4", "5"]:
        a = float(input("Введіть перше число: "))
        b = float(input("Введіть друге число: "))

        if choice == "1":
            print("Результат:", add(a, b))
        elif choice == "2":
            print("Результат:", subtract(a, b))
        elif choice == "3":
            print("Результат:", multiply(a, b))
        elif choice == "4":
            print("Результат:", divide(a, b))
        elif choice == "5":
            print("Результат:", power(a, b))

    elif choice == "6":
        a = float(input("Введіть число: "))
        print("Результат:", sqrt2(a))

    elif choice == "7":
        a = float(input("Введіть число: "))
        print("Результат:", sqrt3(a))
    else:
        print("Невірний вибір, спробуйте ще раз.")
# Завдання 4
# Створіть програму, яка складається з функції, яка приймає три числа і повертає їх середнє арифметичне, і
# головного циклу, що запитує у користувача числа і обчислює їх середні значення за допомогою створеної функції.
def average_of_three(a, b, c):
    return (a + b + c) / 3

while True:
    print("Введіть три числа для обчислення середнього арифметичного або qq в будь-яке число для виходу")
    a = input("Перше число: ")
    b = input("Друге число: ")
    c = input("Третє число: ")
    if "qq" in (a, b, c):
        print("Bye.")
        break
    a = float(a)
    b = float(b)
    c = float(c)
    result = average_of_three(a,b,c)
    print(f"Середнє арифметичне: {result}")

# Завдання 5
# Створіть програму, яка приймає як формальні параметри зріст і вагу користувача, обчислює індекс маси тіла і в
# залежності від результату повертає інформаційне повідомлення (маса тіла в нормі, недостатня вага або слідкуйте
# за фігурою). Користувач з клавіатури вводить значення росту та маси тіла та передає ці дані у вигляді фактичних
# параметрів під час виклику функції. Програма працює доти, доки користувач не зупинить її комбінацією символів «off».
def bmi(height, weight):
    index = weight / (height / 100) ** 2  # формула ІМТ
    print(f"Ваш ІМТ: {index}")

    if index < 18.5:
        return "Недостатня вага"
    elif 18.5 <= index < 25:
        return "Маса тіла в нормі"
    else:
        return "Слідкуйте за фігурою"

while True:
    print("Введіть дані для обчислення ІМТ або 'qq' для виходу.")
    height_input = input("Зріст (у см): ")
    if height_input.lower() == "qq":
        print("Bye.")
        break
    weight_input = input("Вага (у кг): ")
    if weight_input.lower() == "qq":
        print("Bye.")
        break
    height = float(height_input)
    weight = float(weight_input)
    result = bmi(height, weight)
    print(result)